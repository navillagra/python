# cadena1 = "Hola Mundo"
# cadena2 = "Bienvenidomaquinola"
# cadena3 = "1216547945483"
# # print(dir(cadena1)) dir es una funcion que nos muestra los metodos que podemos usar con la variable(texto, numeros, listas, etc).

# # dato.metodo() # asi se llama a un metodo
# # resultado = dir(cadena1)
# # resultado = cadena1.upper() # upper() convierte la cadena a mayusculas
# # resultado = cadena1.lower() # lower() convierte la cadena a minusculas
# # resultado = resultado.capitalize() # capitalize() convierte la primera letra de la cadena a mayuscula
# # resultado = cadena1.find("M") # find() busca una cadena dentro de otra y devuelve la posicion en la que se encuentra. Si ponemos "m" nos devuelve -1 porque Python es case sensitive (sencible a mayusculas y minusculas).
# # resultado = cadena1.index("M") # index() busca una cadena dentro de otra y devuelve la posicionen la que se encuentra. Si ponemos "m" nos lanza una escepcion.
# # resultado = cadena3.isnumeric() # isnumeric() devuelve True si la cadena es numerica y False si no lo es. Devuelve True si la cadena es un numero entero o decimal.
# # resultado = cadena2.isalpha() # isalpha() devuelve True si la cadena es alfabetica y False si no lo es. No acepta espacios ni numeros.
# # resultado = cadena1.count("o") # count() cuenta cuantas veces se repite un caracter en una cadena.
# # resultado = len(cadena1) # len() devuelve la longitud de la cadena. len es una funcion, no un metodo.
# # resultado = cadena1.startswith("H") # startswith() devuelve True si la cadena empieza con el caracter que le pasamos como parametro.
# # resultado = cadena1.endswith("o") # endswith() devuelve True si la cadena termina con el caracter que le pasamos como parametro.
# # resultado = cadena1.replace("Hola", "Adios") # replace() reemplaza una cadena por otra. El primer parametro es la cadena que queremos reemplazar y el segundo parametro es la cadena por la que queremos reemplazarla.
# # resultado = cadena1.split(" / ") # split() divide la cadena en una lista de cadenas. El parametro es el caracter por el que queremos dividir la cadena. Si no le pasamos ningun parametro, divide la cadena por espacios.

# print(resultado)